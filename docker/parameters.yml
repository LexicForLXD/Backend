# This file is auto-generated during the composer install
parameters:
    database_driver: pdo_pgsql
    database_host: '%env(CONTAINER_DB_NAME)%'
    database_port: 5432
    database_name: '%env(DB_NAME)%'
    database_user: '%env(DB_USER)%'
    database_password: '%env(DB_PASSWORD)%'
    mailer_transport: smtp
    mailer_host: 127.0.0.1
    mailer_user: null
    mailer_password: null
    secret: '%env(SECRET)%'
    cert_location: /srv/lexic/client.crt
    cert_key_location: /srv/lexic/client.key
    cert_passphrase: null
    jms_serializer.camel_case_naming_strategy.class: JMS\Serializer\Naming\IdenticalPropertyNamingStrategy
    ssh_location: /srv/lexic/ssh.pub
    ssh_key_location: /srv/lexic/ssh
    ssh_passphrase: '%env(SSH_PASSPHRASE)%'
    ssh_user: '%env(SSH_USER)%'
    nagiosUsername: '%env(NAGIOS_USERNAME)%'
    nagiosPassword: '%env(NAGIOS_PASSWORD)%'
    web_frontend_domain: '%env(WEB_FRONTEND_DOMAIN)%'